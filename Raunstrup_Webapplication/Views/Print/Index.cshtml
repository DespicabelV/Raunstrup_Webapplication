@using System.Runtime.Versioning
@model IEnumerable<Raunstrup_Webapplication.Models.ResourceModel>


@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Print/E-mail</h1>

@if (Model == null)
{
    <text>No offer found </text>
}
else
{
    <section class="row mb-3">

        <section class="col-3">

        </section>

        <table class="table table-bordered col-6 text-center">
            @foreach (var resource in Model)
            {
                <tr>
                    <th>ID</th>
                    <td>@resource.Res_ID</td>
                </tr>
                <tr>
                    <th>Title</th>
                    <td>@resource.Name</td>
                </tr>
                <tr>
                    <th>Start Date</th>
                    <td>@resource.Store_Price</td>
                </tr>
                <tr>
                    <th>End Date</th>
                    <td>@resource.Customer_Price</td>
                </tr>
            @*<tr  class="table-bordered">
                    <th>Price</th>
                    <td>@resource.Offer_Price</td>
                </tr>
                <tr  class="table-bordered">
                    <th>Customer ID</th>
                    <td>@resource.Offer</td>
                </tr>*@

            }

        </table>

        @*<section class="col-3">
            BRUG DEN HER PLADS, HVIS NØDVENDIGT ELLER SLET
        </section>*@

    </section>

    <section class="row mb-3">
        
        <section>

        </section>

        <section class="container text-center col-6">

            <button type="submit" class="btn btn-lg btn-block btn-outline-primary">
                Print this
            </button>
        
            <button type="submit" class="btn btn-lg btn-block btn-outline-primary">
                E-mail this
                <a href="mailto:"></a>
            </button>

        </section>
        
        <section>

        </section>

    </section>
}


